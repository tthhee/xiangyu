#include "List_Sample.h"
#include<stdio.h>

void main()
{
	List L;


//	ElementType x;
	L = Polynominal(3);

//	PrintList(L);
/*	PrintList(L);
	printf("enter the element you want to exchange\n");
	scanf("%d", &x);
	P = Find(x, L);
	ExchangePosition_WithNext(P, L); 
	*/
//	Sort(L);
 
//	I = Union(L, P);
	PrintList(L);
}

